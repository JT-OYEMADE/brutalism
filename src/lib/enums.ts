export enum accountType {
  personal = 'personal',
  business = 'business',
}

export enum TransactionType {
  SENT = 'sent',
  EXCHANGE = 'exchange',
  RECEIVED = 'received',
  WITHDRAW = 'withdraw',
}
